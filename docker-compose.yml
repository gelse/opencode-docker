services:
  opencode:
    build:
      context: ./build
      dockerfile: Dockerfile
    image: opencode:latest
    volumes:
      - /:/host:ro
      - opencode_data:/root/.opencode
    stdin_open: true
    tty: true

volumes:
  opencode_data:
